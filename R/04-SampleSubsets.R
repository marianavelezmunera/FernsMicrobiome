# Subsets by sample type 

#Bacteria

bacteria_phyllosphere<-subset_samples(bacteria_rare,Sample_type=="Phyllosphere")
bacteria_soil<-subset_samples(bacteria_rare,Sample_type=="Soil")
bacteria_rhizosphere<-subset_samples(bacteria_rare,Sample_type=="Rhizosphere")

# Fungi

fungi_phyllosphere<-subset_samples(fungi_rare,Sample_type=="Phyllosphere")
fungi_soil<-subset_samples(fungi_rare,Sample_type=="Soil")
fungi_rhizosphere<-subset_samples(fungi_rare,Sample_type=="Rhizosphere")

